import { DashboardStats, Vehicle, Proposal } from "@/types";

// Mock data for the dashboard stats
export const dashboardStats: DashboardStats = {
  totalVehicles: 32,
  soldVehicles: 18,
  reservedVehicles: 5,
  totalProposals: 47,
  pendingProposals: 12,
  contactedProposals: 23,
  closedProposals: 12,
  topBrand: {
    name: "Volkswagen",
    count: 8
  },
  newProposals: 5
};

// Other mock data can be added here as needed
